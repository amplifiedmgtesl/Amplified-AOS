import { AppShell } from "@/components/layout/app-shell"; export default function Page(){ return <AppShell title="Crew" subtitle="Use Employee Directory and Job Sheets for roster and assignment workflows."><div className="card"><h2 className="section-title">Crew</h2><p className="muted">The National Employee Directory page now serves as the main crew database for this stable rebuild.</p></div></AppShell>; }
