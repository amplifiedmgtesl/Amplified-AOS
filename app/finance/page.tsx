import { AppShell } from "@/components/layout/app-shell"; export default function Page(){ return <AppShell title="Finance" subtitle="Use the Invoices page for professional billing and PDF output."><div className="card"><h2 className="section-title">Finance</h2><p className="muted">This rebuild centers finance around the Invoices page so invoice generation stays quick and stable.</p></div></AppShell>; }
