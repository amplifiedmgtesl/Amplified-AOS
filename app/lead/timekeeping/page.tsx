import Timekeeping from "@/components/shared/timekeeping";

export const metadata = { title: "Timekeeping — Crew Leader" };

export default function LeadTimekeepingPage() {
  return <Timekeeping hidePayAlways />;
}
