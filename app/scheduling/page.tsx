import { AppShell } from "@/components/layout/app-shell"; export default function Page(){ return <AppShell title="Scheduling" subtitle="Use Master Calendar and Job Sheets for scheduling in this rebuild."><div className="card"><h2 className="section-title">Scheduling</h2><p className="muted">The Master Calendar and Job Sheets pages now handle the core scheduling workflow.</p></div></AppShell>; }
